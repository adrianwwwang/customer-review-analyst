# Customer Review Analyst — Python dependencies
#
# The HTML dashboard uses Chart.js via CDN — no Python packages needed for the core output.
# The packages below are only required for optional output formats.
#
# Install all optional dependencies at once:
#   pip install -r requirements.txt
#
# Or install only what you need:

# --- Required for PowerPoint slides (.pptx) ---
python-pptx>=0.6.21
matplotlib>=3.7.0

# --- Required for PDF output ---
# Choose one:
weasyprint>=60.0
# pdfkit>=1.0.0        # alternative — also requires wkhtmltopdf system package
